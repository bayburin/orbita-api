require 'spec_helper'

# shared_examples_for 'requirementable' do
#   context 'when Claim does not exist' do
#     let!(:claim) { nil }
#     # before { allow_any_instance_of(Event).to receive(:claim).and_return(nil) }
#     # before { allow(event).to receive(:claim).and_return(nil) }

#     it { expect(context).to be_a_failure }
#     it { expect(context.error).to eq 'Заявка не найдена' }
#   end

#   context 'when User does not exist' do
#     let!(:user) { nil }
#     # before { allow_any_instance_of(Event).to receive(:user).and_return(nil) }

#     it { expect(context).to be_a_failure }
#     it { expect(context.error).to eq 'Пользователь не найден' }
#   end
# end
